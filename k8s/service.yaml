apiVersion: v1
kind: Service
metadata:
  name: docuchat-svc
  namespace: docuchat
  labels:
    app: docuchat
spec:
  type: NodePort
  selector:
    app: docuchat
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000